# Generated large-document fixture

Run `npm run stress:generate`. The deterministic approximately 100,000-line fixture is written to `public/stress-fixtures/large-scripted-100000.html`, which is intentionally gitignored and excluded from the VSIX.
